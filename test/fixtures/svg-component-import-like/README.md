This fixture intentionally contains SVG component imports
(`ReactComponent`, `?react`, `?vue`, `?component`) to verify the
`avoid-svg-component-imports` rule fires correctly.

Findings from this directory during dogfooding or testing are expected.
The imports here are deliberately problematic — do not treat them as code to fix.

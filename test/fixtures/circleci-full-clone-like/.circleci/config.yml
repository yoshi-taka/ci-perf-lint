version: 2.1

jobs:
  build:
    docker:
      - image: cimg/node:18.0
    steps:
      - checkout:
          method: full
      - run: npm ci
      - run: npm test

  deploy:
    docker:
      - image: cimg/node:18.0
    steps:
      - checkout
      - run: ./deploy.sh

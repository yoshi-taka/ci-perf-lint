name: release

on:
  push:
    tags:
      - "v*"

jobs:
  github_release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Read release metadata
        run: gh release view "$GITHUB_REF_NAME" --json tagName,name,body

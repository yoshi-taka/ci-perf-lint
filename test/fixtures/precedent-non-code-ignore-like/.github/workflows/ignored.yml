name: ignored
on:
  pull_request:
    paths-ignore:
      - "**/*.md"
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npm ci
      - run: npm run build

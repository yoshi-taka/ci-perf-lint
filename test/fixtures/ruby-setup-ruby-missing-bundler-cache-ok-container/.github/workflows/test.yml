name: Rails CI
on: pull_request

jobs:
  test:
    runs-on: ubuntu-latest
    container: ruby:3.3-slim
    steps:
      - uses: actions/checkout@v4
      - uses: ruby/setup-ruby@v1
        with:
          ruby-version: "3.3"
      - run: bundle install
      - run: bundle exec rspec

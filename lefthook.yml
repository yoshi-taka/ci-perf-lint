pre-commit:
  commands:
    oxlint:
      glob: "{src,test}/*.ts"
      run: oxlint --type-aware --fix
    audit-static:
      glob: "{src,test}/*.ts"
      run: bun run audit:static
    config-audit:
      glob: "{knip.json,.fallowrc.json}"
      run: bun run audit:static
